# Sprint S4: Gemini parse_materials migration + first real lab — **PAUSED (2026-04-20)**

> **Status: PAUSED.** Subsumed by the Northstar Rebuild plan at `.claude/plans/get-200-in-optimized-swing.md`. T1, T2, T3, T6 already landed (commits `e26bd35`, `1198cde`) and their outputs feed S6 (generator rewrite). **Do NOT execute T4, T5, or T7 against the current text-only pipeline — S6 rewrites `generate_lab` to produce Sandpack React, and the parse-materials rewrite lives inside that sprint.** When reopening S4, reconcile against the new sprints: S4' (slug + schema), S5 (retrieval), S6 (generator), S7 (frontend).

**Original goal**: Swap the OOM-prone Claude-vision rasterization pipeline for Gemini 2.5 Flash's Files API, then drive a real upload all the way through parse → propose_plan → approve → generate_lab to produce the first actual lab on Railway free tier.

**Estimated sessions**: 3–4 (most weight on S4-T4 rewrite and S4-T7 integration)

**Prerequisites (already done — do NOT redo)**:
- `GEMINI_API_KEY` is present in `.env.local.example` and in the Railway/production env. No env-wiring task below.
- `sweepStuckJobs()` + 2h `Promise.race` job timeout shipped in commit `487be4b` (`worker/lib/job-runner.ts`, `worker/index.ts`). No sweep task below.
- Supabase migration `005_source_materials_bucket.sql` is applied in prod; uploads succeed today.
- Plan: `.claude/plans/gemini-parse-materials-migration.md` — authoritative spec; re-read only the "Implementation approach for parse-materials.ts" and "Cost comparison" sections if needed.

**Out of scope (from the plan, reaffirmed)**:
- DOCX/PPTX native support (T4 will fail these with a clear message and add tech-debt #5).
- Migrating `generate_lab`, `evaluate_review`, or `generate_embeddings` to Gemini.
- Provider abstraction layer.

**Scope callout from user**: "We have not gotten an actual lab to generate yet." This means the sprint is not complete when `parse_materials` exits 0 — the integration check in S4-T7 must produce a `labs` row with non-empty `content` for a real uploaded PDF.

---

## Task dependency graph

```
S4-T1 (SDK + client) ─┐
                      ├─> S4-T4 (rewrite parse-materials) ─> S4-T5 (CLAUDE.md + tech-debt) ─> S4-T7 (integration)
S4-T2 (shared schema)─┤                                                                        ▲
                      │                                                                        │
S4-T3 (cost tracker) ─┘                                                                        │
                                                                                               │
S4-T6 (downstream smoke on mocked parse output) ───────────────────────────────────────────────┘
```

S4-T1, S4-T2, S4-T3, S4-T6 can run in parallel. S4-T4 waits on T1+T2+T3. S4-T5 waits on T4. S4-T7 is last.

---

## Tasks

### S4-T1: Install Gemini SDK and create worker client module

- **Depends on**: none
- **Files**:
  - `worker/package.json` (add dep)
  - `worker/package-lock.json` (regenerated by npm install)
  - `worker/lib/gemini.ts` (new)
- **Schema tables**: none
- **Agent mode**: `/implement`
- **Context to load**:
  - `worker/lib/supabase.ts` (mirror the client-module shape)
  - `worker/package.json` (current deps)
- **Token budget**: small
- **Verification**:
  ```bash
  cd worker && npm install && node -e "require('@google/generative-ai'); console.log('PASS')"
  cd worker && npx tsc --noEmit && echo "TSC PASS"
  ```
- **Acceptance criteria**:
  - [ ] `@google/generative-ai` is a runtime dependency in `worker/package.json` (not devDependencies)
  - [ ] `worker/lib/gemini.ts` exports initialized `geminiFlash` and `geminiPro` client instances reading `process.env.GEMINI_API_KEY`
  - [ ] Module throws on import if `GEMINI_API_KEY` is unset, matching the fail-fast pattern in `worker/lib/supabase.ts`
  - [ ] `tsc --noEmit` clean

---

### S4-T2: Add whole-document output schema to shared package

- **Depends on**: none
- **Files**: `packages/shared/src/generation.ts`
- **Schema tables**: none (pure TypeScript/Zod)
- **Agent mode**: `/implement`
- **Context to load**:
  - `packages/shared/src/generation.ts` (lines 20–36 for the existing `parsedBlockSchema` + `parsedPageSchema`)
  - `SCHEMA.md` lines 615–675 (Generation Pipeline Output Schema — check if it documents the shape we're adding)
- **Token budget**: small
- **Verification**:
  ```bash
  cd packages/shared && npx tsc --noEmit && echo "TSC PASS"
  # Ensure existing consumers still compile:
  cd ../.. && npx tsc --noEmit && echo "ROOT TSC PASS"
  ```
- **Acceptance criteria**:
  - [ ] New export `parsedDocumentSchema` with shape `{ pages: ParsedPage[] }` is added
  - [ ] `parsedBlockSchema`, `parsedPageSchema`, `parseMaterialsPayloadSchema` remain unchanged and exported (nothing breaks for current callers)
  - [ ] Inferred type `ParsedDocument` is exported
  - [ ] No widening of existing schemas (Gemini response schema mode has nesting/type limits — if you hit them, document the workaround in a code comment rather than mutating shared types)

---

### S4-T3: Add Gemini pricing constants + material_parsing usage helper

- **Depends on**: none
- **Files**: `worker/lib/cost-tracker.ts`
- **Schema tables**: `api_usage_log` (SCHEMA.md lines 483–502 only)
- **Agent mode**: `/implement`
- **Context to load**:
  - `worker/lib/cost-tracker.ts` (entire file, 37 lines)
  - `SCHEMA.md` lines 483–502 (api_usage_log columns)
- **Token budget**: small
- **Verification**:
  ```bash
  cd worker && npx tsc --noEmit && echo "TSC PASS"
  ```
  Additionally, a 5-line inline assertion at the bottom of the test file (or a `tests/cost-tracker.test.ts`) confirming `calcGeminiCostCents('gemini-2.5-flash', 1_000_000, 1_000_000)` returns the expected number of cents given the pricing constants.
- **Acceptance criteria**:
  - [ ] Add `GEMINI_PRICING` constant: `{ 'gemini-2.5-flash': { inputPerMillion: 0.30, outputPerMillion: 2.50 }, 'gemini-2.5-pro': { inputPerMillion: 1.25, outputPerMillion: 10.00 } }` (dollars)
  - [ ] Add `calcGeminiCostCents(model, inputTokens, outputTokens)` helper that returns an integer number of cents (rounded up; never negative)
  - [ ] Existing `trackUsage` signature unchanged — `parse-materials.ts` will compute cost at the call site and pass the number in
  - [ ] **Rate-check at start of task**: `WebFetch` ai.google.dev/pricing; if rates differ from the numbers above, update the constants AND leave a comment with the date checked. Do not silently ship stale pricing.

---

### S4-T4: Rewrite parse-materials.ts to use Gemini Files API

- **Depends on**: S4-T1, S4-T2, S4-T3
- **Files**: `worker/processors/parse-materials.ts`
- **Schema tables**:
  - `source_materials` (SCHEMA.md lines 181–197)
  - `content_blocks` (SCHEMA.md lines 198–216)
  - `generation_jobs` (SCHEMA.md lines 407–437)
- **Agent mode**: `/implement`
- **Context to load**:
  - `worker/processors/parse-materials.ts` (entire current file, 282 lines — need to know what to delete AND what to keep: the storage download, DB inserts, progress updates, prompt text)
  - `worker/lib/gemini.ts` (from T1)
  - `packages/shared/src/generation.ts` lines 20–40 (existing schemas) + the new `parsedDocumentSchema` from T2
  - `.claude/plans/gemini-parse-materials-migration.md` — "Implementation approach for `parse-materials.ts`" section only
  - `SCHEMA.md` lines 181–216 (source_materials + content_blocks)
- **Token budget**: large
- **Verification**:
  ```bash
  cd worker && npx tsc --noEmit && echo "TSC PASS"
  # Static sanity: the rewrite must not reimport pdf-to-img:
  ! grep -q "pdf-to-img" worker/processors/parse-materials.ts && echo "NO PDF-TO-IMG PASS"
  # And must reference the Gemini client:
  grep -q "from '../lib/gemini" worker/processors/parse-materials.ts && echo "GEMINI IMPORT PASS"
  ```
- **Acceptance criteria**:
  - [ ] All `pdf-to-img` / page-rasterization code is removed. Buffer accumulation (`const pages: Uint8Array[] = []`) is gone.
  - [ ] Flow: download from Supabase Storage → upload buffer to Gemini Files API → single `generateContent` call with `responseMimeType: 'application/json'` and `responseSchema: parsedDocumentSchema` → validate response with Zod → insert `content_blocks` rows → delete the Gemini file (success AND failure paths)
  - [ ] MIME gate: only `application/pdf`, `image/png`, `image/jpeg` proceed. DOCX/PPTX throws `Error('DOCX/PPTX parsing not yet supported — see tasks/tech-debt.md #5')` so the job row gets a clear `error_message`.
  - [ ] `updateProgress` calls at: 10% (uploading to Gemini), 40% (generating), 75% (parsing response), 95% (inserting blocks). No per-page progress (we're no longer looping pages).
  - [ ] `trackUsage` called after `generateContent` returns, pulling `response.usageMetadata.promptTokenCount` and `candidatesTokenCount`, computing cost via `calcGeminiCostCents('gemini-2.5-flash', ...)`.
  - [ ] Gemini file cleanup is in a `finally` block — do not leak uploaded files on error.
  - [ ] `extractJson` fallback (if it exists in the current file) is kept as a belt-and-suspenders — response schema mode usually returns clean JSON but Gemini can wrap in prose.
  - [ ] **Memory target**: for a 30-page 10MB PDF, peak RSS must stay under 200MB. Not a CI check; verified by eye in the T7 Railway smoke.

---

### S4-T5: Update CLAUDE.md env vars + append tech-debt entries #4 and #5

- **Depends on**: S4-T4
- **Files**:
  - `CLAUDE.md` (env var list + "Generation Pipeline Architecture" paragraph)
  - `tasks/tech-debt.md` (append entries #4 and #5)
- **Schema tables**: none
- **Agent mode**: `/implement`
- **Context to load**:
  - `CLAUDE.md` (search for "Environment Variables" and "Generation Pipeline Architecture")
  - `tasks/tech-debt.md` (entire file — it's short; know the existing entry format)
- **Token budget**: small
- **Verification**:
  ```bash
  grep -q "GEMINI_API_KEY" CLAUDE.md && echo "ENV DOC PASS"
  grep -q "^## 4\." tasks/tech-debt.md && grep -q "^## 5\." tasks/tech-debt.md && echo "TECH DEBT PASS"
  ```
- **Acceptance criteria**:
  - [ ] `CLAUDE.md` env var section lists `GEMINI_API_KEY` with a one-liner on its role
  - [ ] `CLAUDE.md` pipeline paragraph notes parse_materials runs on Gemini 2.5 Flash via Files API
  - [ ] `tasks/tech-debt.md` entry #4: `pdf-to-img` removed from the tree (retained previously for a Claude-vision fallback; now dead code, safe to purge worker deps on next cleanup) — document why it was kept transiently and when to drop the dep entry from `worker/package.json`
  - [ ] `tasks/tech-debt.md` entry #5: DOCX/PPTX parsing unsupported. List the two options (LibreOffice → PDF in Dockerfile, or text-extract via `mammoth`/`pptx-parser` accepting loss of visuals). Don't pick one — it's a decision item.

---

### S4-T6: Downstream smoke — mocked parse output feeds propose_plan + generate_lab

- **Depends on**: none (can run in parallel with T1-T4)
- **Files**: `tests/generation-pipeline-smoke.ts` (new)
- **Schema tables**:
  - `source_materials`, `content_blocks`, `generation_jobs`, `generation_plans`, `labs`, `modules` (SCHEMA.md lines 143–216, 407–480)
- **Agent mode**: `/implement`
- **Context to load**:
  - `worker/processors/propose-plan.ts` (just skim signatures + DB reads to know what inputs it needs)
  - `worker/processors/generate-lab.ts` (same)
  - `SCHEMA.md` lines 143–180 (modules + labs), 438–480 (generation_plans)
- **Token budget**: medium
- **Verification**:
  ```bash
  npx tsx tests/generation-pipeline-smoke.ts && echo "SMOKE PASS"
  ```
  The script:
  1. Inserts a throwaway test course + source_material row owned by a test user
  2. Inserts fake `content_blocks` rows (10–15 blocks across 3 pages, covering heading/paragraph/equation types) as if parse_materials had succeeded
  3. Enqueues a `propose_plan` job and waits up to 60s for it to complete
  4. Reads back the resulting `generation_plans` row; asserts shape matches `planDataSchema`
  5. Simulates professor approval: creates module + lab rows, enqueues a `generate_lab` job
  6. Waits up to 180s for `generate_lab` to complete
  7. Asserts the resulting `labs.content` row is non-null and matches `labContentSchema`
  8. Cleans up all test rows
- **Acceptance criteria**:
  - [ ] Script runs against local Supabase with the real Railway worker running against the same project (or local `worker/index.ts`)
  - [ ] Exits 0 only when a `labs.content` row exists and validates
  - [ ] If this fails, it means the downstream pipeline has bugs independent of parse_materials — report them as new `tasks/todo.md` items before moving to T7. This task is the gating check for "the pipeline works end-to-end given clean parse output."

---

### S4-T7: Integration check — real PDF → real lab on Railway

- **Depends on**: S4-T4, S4-T5, S4-T6
- **Files**: none (manual + observation)
- **Schema tables**: all of the above
- **Agent mode**: manual verification (no `/implement`)
- **Context to load**: none
- **Token budget**: small
- **Verification**:
  1. Deploy merged sprint to Railway; confirm boot log shows `[worker] Starting poll loop...` with no sweep errors.
  2. Sign in as Professor Pat → create a fresh course → upload a real PDF (suggestion: a 10–30 page lecture notes PDF from the Miami pilot set — NOT a 100+ page textbook on the first run).
  3. Observe Railway logs: `[parse-materials] Uploading to Gemini Files API` → `[parse-materials] Generating content` → `[parse-materials] Inserted N content_blocks` → `job completed`. No exit 137.
  4. Railway metrics: peak memory under 200MB.
  5. In the UI, the Map/Review screen renders with a generation_plan; approve it.
  6. Wait for `generate_lab` jobs to complete (Railway logs + UI spinner).
  7. Navigate to the student-facing lab view → lab content is non-empty and renders.
  8. Spot-check Supabase:
     ```sql
     SELECT id, status, error_message, actual_cost_cents
     FROM generation_jobs
     WHERE course_id = '<the test course id>'
     ORDER BY created_at;
     ```
     Expect all rows `completed` with non-zero `actual_cost_cents`.
     ```sql
     SELECT model, input_tokens, output_tokens, cost_cents
     FROM api_usage_log
     WHERE generation_job_id IN (SELECT id FROM generation_jobs WHERE course_id = '<id>');
     ```
     Expect rows with `model = 'gemini-2.5-flash'` and sensible token counts.
- **Acceptance criteria**:
  - [ ] Railway worker boots and processes the upload without OOM
  - [ ] `api_usage_log` contains at least one `gemini-2.5-flash` row for the parse job
  - [ ] A `labs` row exists with non-empty `content` JSON matching `labContentSchema`
  - [ ] Total LLM cost for the end-to-end flow (from `api_usage_log` SUM) is logged in the sprint review doc (`s4-gemini-parse-materials-review.md`) for baseline future comparison
  - [ ] **This is the sprint's success criterion**. Per user: "we have not gotten an actual lab to generate yet." Sprint is not complete until this box is checked.

---

## Sprint review output (create on completion)

`.claude/sprints/s4-gemini-parse-materials-review.md` with:
- Status: Complete / Blocked
- Actual cost of the first generated lab (token counts + cents)
- Peak worker memory during T7
- Any downstream issues surfaced by T6 that were fixed inline vs. deferred to new todo items
- Date completed

Follow the convention established by `s3-worker-processors-review.md`.
